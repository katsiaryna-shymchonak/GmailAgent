version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ai-gmail-agent:dev
    container_name: gmail_agent
    ports:
      - "8000:8000"
    env_file:
      - server/.env
    restart: unless-stopped
